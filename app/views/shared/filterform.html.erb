<div class="search-box">
  <h3 class="follow">Filter By:</h3>
  <form action="<%= all_politicians_path %>" method="GET" id="filter" class="form-inline">
	    <ul>
	    	<li class="styled-select">
	    		<label for="state">State:</label>
				<select name="state" id="state_filter" class="politician-select">
					<option value="">---------</option>
                    <% @states.each do |st| %>
                        <% if st != '' and st != ' ' and st %>
    					  <option value="<%= st %>" <% if @filters['state'] == st %> selected <% end %> ><%= st %></option>
                        <% end %>
                    <% end %>
				</select>
			</li>
			<li class="styled-select">
	    		<label for="party">Party:</label>
				<select name="party" id="party_select" class="politician-select">
					<option value="" >---------</option>
                    <% @parties.each do |par| %>
        				<option value="<%=par.name%>" <% if @filters['party'] == par.name %> selected <% end %>><%=par.display_name%></option>
                    <% end %>
				</select>
			</li>
			<li class="styled-select">
	    		<label for="office">Position:</label>
				<select name="office" id="office_select" class="politician-select">
					<option value="">---------</option>
                    <% @offices.each do |off| %>
    					<option value="<%= off.id %>" <%if @filters['office'].to_f == off.id %> selected <% end %>>
                            <%=off.title %>
                        </option>
                    <% end %>
				</select>
			</li>
	    <li><button type="submit" class="btn btn-primary">Filter</button></li>
		</ul>
	</form>
</div>
